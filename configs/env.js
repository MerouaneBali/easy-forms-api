// const dotenv = require("dotenv");

// const { envErrorHandler, envParsedHandler } = require("../utils/handlers");

// const env = dotenv.config();

// env.error ? envErrorHandler(env.error) : envParsedHandler(env.parsed);

const env = {};

module.exports = env;
